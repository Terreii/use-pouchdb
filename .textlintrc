{
  "filters": {},
  "rules": {
    "alex": {
      "allow": [
        special,
        reject,
        rejects,
        hook,
        hooks,
        destroy,
        destroys
      ]
    },
    "common-misspellings": true
  }
}
