{
  "filters": {},
  "rules": {
    "alex": {
      "allow": [
        reject,
        rejects,
        hook,
        hooks,
        destroy,
        destroys
      ]
    },
    "common-misspellings": true
  }
}
